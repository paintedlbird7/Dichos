<!-- // Homepage -->
<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <link rel="stylesheet" href="/stylesheets/style.css">
  <title>Your Quotes</title>
</head>

<body>
  <%- include('../partials/_navbar.ejs') %>
    <div class="container">
      <h3 class="center-align red-text text-darken-2">Your Quotes/ Dichos</h3>
      <div class="center-align">
        <a href="/users/<%= user._id %>/dichos/new" class="btn red lighten-2">
          <i class="material-icons left" alt="plus sign to add">add</i> Add a Quote
        </a>
      </div>
      <div class="row">
        <% dichos.forEach((dicho)=> { %>
          <div class="col s12 m6 l4">
            <div class="card">
              <div class="card-content">
                <span class="card-title">
                  <%= dicho.title %>
                </span>
                <p>
                  <%= dicho.content %>
                </p>
                <p class="grey-text"> By <%= dicho.notes %>
                </p>
                <p>
                  <%= dicho.status %>
                </p>
              </div>
              <div class="card-action">
                <a href="/users/<%= user._id %>/dichos/<%= dicho._id %>" class="red-text">View</a>
                <a href="/users/<%= user._id %>/dichos/<%= dicho._id %>/edit" class="blue-text">Edit</a>
                <form action="/users/<%= user._id %>/dichos/<%= dicho._id %>?_method=DELETE" method="POST"
                  style="display:inline;">
                  <button type="submit" class="btn-flat red-text">Delete</button>
                </form>
              </div>
            </div>
          </div>
          <% }) %>
      </div>
    </div>
    <footer>
      <p>2025 Rosa Perez | Painted Bird Designs
        <a href="https://github.com/paintedlbird7" target="_blank">GitHub</a> |
        <a href="https://www.rosapérez.com">Portfolio</a>
      </p>
    </footer>
</body>

</html>